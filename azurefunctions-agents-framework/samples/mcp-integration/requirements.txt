# Core dependencies for MCP integration examples
azurefunctions-agent-framework

# MCP dependencies
mcp>=1.0.0

# Additional dependencies for specific examples
requests>=2.31.0
fastapi>=0.104.0
uvicorn>=0.24.0

# Git MCP server (optional - can be installed separately)
# mcp-server-git

# Development dependencies
python-dotenv>=1.0.0
