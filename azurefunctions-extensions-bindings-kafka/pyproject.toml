[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "azurefunctions-extensions-bindings-kafka"
dynamic = ["version"]
requires-python = ">=3.9"
authors = [{ name = "Azure Functions team at Microsoft Corp.", email = "azurefunctions@microsoft.com"}]
description = "Kafka Python worker extension for Azure Functions."
readme = "README.md"
license = {text = "MIT License"}
classifiers= [
        'License :: OSI Approved :: MIT License',
        'Intended Audience :: Developers',
        'Programming Language :: Python :: 3',
        'Programming Language :: Python :: 3.10',
        'Programming Language :: Python :: 3.11',
        'Programming Language :: Python :: 3.12',
        'Programming Language :: Python :: 3.13',
        'Operating System :: Microsoft :: Windows',
        'Operating System :: POSIX',
        'Operating System :: MacOS :: MacOS X',
        'Environment :: Web Environment',
        'Development Status :: 4 - Beta',
    ]
dependencies = [
    'azurefunctions-extensions-base>=1.1.0',
    'protobuf>=4.0.0,<6.0'
    ]

[project.optional-dependencies]
dev = [
    'pytest',
    'pytest-cov',
    'coverage',
    'pytest-instafail',
    'pre-commit',
    'mypy',
    'flake8'
    ]

[tool.setuptools.dynamic]
version = {attr = "azurefunctions.extensions.bindings.kafka.__version__"}

[tool.setuptools.packages.find]
exclude = [
    'azurefunctions.extensions.bindings','azurefunctions.extensions',
    'azurefunctions', 'tests', 'samples'
    ]
